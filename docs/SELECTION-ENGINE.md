# Selection Engine

Reject candidates that fail required assets, capacity, minimum type, contrast, or safe-zone gates. Score remaining layouts by information structure, asset fit, capacity, hierarchy, density, format, safe-zone confidence, historical preference, and diversity.

Channel identity is not a primary layout-selection feature. Select a clear winner automatically; render the top three with identical copy when scores are close; report a concrete unmet need when no candidate is suitable.
