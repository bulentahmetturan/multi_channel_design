name: Hekimler Python source runner

# Production scheduler for the Hekimler sources that do not fit the Cloudflare Workers Free CPU budget
# (execution: python_runner). Each source runs in its own process with a hard timeout and bounded retry;
# one failing source never fails the batch. Results are ingested through the authenticated Hub endpoints.

on:
  workflow_dispatch:
    inputs:
      sources:
        description: "all-python | all | comma-separated source ids"
        required: false
        default: "all-python"
      timeout:
        description: "Per-source timeout in seconds"
        required: false
        default: "240"
      retries:
        description: "Retries per source on transient failures"
        required: false
        default: "2"
      dry_run:
        description: "Dry run (no ingest)"
        required: false
        type: boolean
        default: false
  schedule:
    - cron: "17 4 * * *"   # daily, 04:17 UTC

permissions:
  contents: read

concurrency:
  group: hekimler-python-runner
  cancel-in-progress: false   # never overlap two runs

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    defaults:
      run:
        working-directory: channels/tip-ogrencileri-platformu
    env:
      GCOS_HUB_URL: https://global-content-os.channel-content-os-mcp.workers.dev
      HEKIMLER_CONTINUOUS_INGESTION_ENABLED: "true"
      PYTHONIOENCODING: utf-8
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pyyaml

      - name: Run sources
        env:
          TIP_RADAR_INGEST_TOKEN: ${{ secrets.TIP_RADAR_INGEST_TOKEN }}
          SOURCES: ${{ github.event.inputs.sources || 'all-python' }}
          SOURCE_TIMEOUT: ${{ github.event.inputs.timeout || '240' }}
          SOURCE_RETRIES: ${{ github.event.inputs.retries || '2' }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: |
          EXTRA=""
          if [ "$DRY_RUN" = "true" ]; then EXTRA="--dry-run"; fi
          python scripts/hekimler_scheduled_run.py \
            --sources "$SOURCES" --timeout "$SOURCE_TIMEOUT" --retries "$SOURCE_RETRIES" \
            --report-dir report $EXTRA

      - name: Upload run report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hekimler-run-report
          path: channels/tip-ogrencileri-platformu/report/
          retention-days: 30
