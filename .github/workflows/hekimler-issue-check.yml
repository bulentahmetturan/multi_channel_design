name: Hekimler sorun tespit listesi

# Daily read-only check of the production Hub against content/SORUN-TESPIT-LISTESI.md.
# Red run (GitHub e-mail) when any internal check FAILs; external blockers (HSGM, GMC, ECFMG, MiG) do not fail it.

on:
  workflow_dispatch:
  schedule:
    - cron: "27 7 * * *"

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python channels/tip-ogrencileri-platformu/scripts/hekimler_issue_check.py
