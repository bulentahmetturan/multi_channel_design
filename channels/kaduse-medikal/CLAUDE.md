# Kaduse Medikal — Claude

Read `AGENTS.md` (this directory) as the canonical instructions for this channel's visual, design, composition, and rendering work. Follow it exactly as written.

`VISUAL-SYSTEM.md` was removed in the 2026-09-16 visual system reset — see `AGENTS.md`'s own "Visual system reset" section and `channel-content-os/VISUAL_SYSTEM_RESET.md`. The first replacement format, `KAD-PP-SPHF-01` v1.0.0, is now implemented — see `AGENTS.md`'s "Update, 2026-09-16" note and `channel-content-os/KAD-PP-SPHF-01.md`. It covers only `product-promotion` / single-SKU-hero content; do not invent rules for anything outside its declared scope. No Claude-specific design rules exist separately from `AGENTS.md` — if something needs to change, that happens through the FORMAT SPEC (`channel-content-os`'s `format-spec/format-registry.ts`) once authorized, not a parallel Claude-only document.
