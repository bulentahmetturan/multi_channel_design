# Moved

All design-reference/Design Universe work (VoltAgent catalog, Guizang layouts/themes,
post concepts, selection engine) now lives in the `channel-content-os` repo:
https://github.com/bulentahmetturan/channel-content-os

- Static catalog files: `design-catalog/`
- Live queryable data + selection engine: Cloudflare D1, via the deployed MCP server
  (`mcp-server/`)

Moved here 2026-09-02 per explicit decision to consolidate all design-system work into
one place going forward, rather than splitting it between this repo and channel-content-os.
